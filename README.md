# macOS Rich Clipboard Manager
A lightweight Python utility for macOS that stores the last 3 copied items (Text, Images, Files) and allows you to select them via a global hotkey (`Ctrl + Shift + V`).

## Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Run the script: `python3 clipboard_manager.py`
